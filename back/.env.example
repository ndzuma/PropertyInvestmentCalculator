# Backend Environment Variables for Property Investment Calculator
# Copy this file to .env and update the values as needed

# Server Configuration
# Port the server will listen on (Railway sets this automatically)
PORT=8000

# Host to bind to (use 0.0.0.0 for Railway deployment)
HOSTURL=0.0.0.0

# Whether to enable auto-reload for development (set to false in production)
RELOAD=true

# CORS Configuration
# Allowed origins for cross-origin requests
# Use "*" to allow all origins (development only)
# For production, specify exact origins separated by commas
# Example: https://your-frontend.railway.app,http://localhost:3000
CORS_ORIGINS=*

# Development Example:
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Production Example:
# CORS_ORIGINS=https://your-frontend-domain.railway.app

# Logging Configuration
# Log level for the application (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Railway Deployment Notes:
# - PORT is automatically set by Railway, but you can override it
# - HOSTURL should be 0.0.0.0 for Railway deployment
# - RELOAD should be false in production
# - CORS_ORIGINS should specify your frontend domain(s) in production
